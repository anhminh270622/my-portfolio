/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'spotify-profile.example.com',
      'halcyon-theme.netlify.app'
    ],
  },
}

module.exports = nextConfig 